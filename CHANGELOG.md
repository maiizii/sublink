# Changelog

本文件用于记录 SubLink（yet.la）管理平台的变更。以下条目初始化了「服务端重定向」基线任务，方便后续迭代追踪。

## [v1.10.8.3] - 2025-10-15

### 文档
- README 更新至 v1.10.8.3，补充多域名治理说明并强调首个域名是默认短链与所有外显文案使用的主域，避免多域名场景下的品牌混淆。
- ONBOARDING 指南同步描述主域职责及多域名配置策略，便于新成员理解首域名的显示逻辑。

## [v1.10.8.2] - 2025-10-12

### 新增
- 新增 `subdomain_blacklist` 数据表与默认保留前缀，启动时会自动写入 `www`、`mail`、`api`、`admin` 等常见敏感标签，管理员可通过后台或 `/api/subdomain-blacklist` 接口维护。
- 管理后台「设置」页加入子域屏蔽名单卡片，支持一次性粘贴空格分隔的前缀并触发 HTMX 表格刷新，实现批量更新。
- 支持在站点设置中维护多域名列表（`managed_domains`），创建短链时可选择域名且会校验是否在管理范围内。

### 变更
- 普通用户创建或修改子域时会校验前缀长度需在 3-20 个字符之间，并拒绝使用屏蔽名单中的任何前缀；管理员保留覆盖能力以便处理紧急需求。
- 子域与屏蔽名单相关的 API 返回信息优化，确保被拒操作会展示明确的字段错误或冲突提示。

## [v1.10.6] - 2025-10-09

### 新增
- 管理后台提供全新的「设置」页，管理员可直接在界面上配置基础域名、短链默认长度、路径前缀以及站点的 Logo/Icon。
- 暴露 `/api/settings` 读写接口，支持 HTMX 表单与常规 JSON/表单调用，便于自动化脚本同步站点配置。

### 变更
- 站点品牌信息（标题、页脚版权、Logo/Icon）与短链前缀从数据库读取，默认值自动写入且可在运行时更新，无需维护 `.env` 文件。
- 短链捕获逻辑支持自定义路径前缀（如 `/r/`），更新后立即影响前端展示与跳转行为。
- README 与部署文档更新至 v1.10.6，新增关于设置页面与数据库配置的说明。

## [v0.1.0] - 2025-10-07（候选）

> 该候选版本聚合了 `server-side redirects` 初始能力及与之配套的部署、接口与运维脚本。

### 2025-10-08
- 新增 `users` 数据表与权限模型，支持区分管理员与普通用户并记录资源归属。
- 管理后台引入用户管理页面，可创建、编辑、删除用户并强制保留至少一名管理员。
- 后端提供 `POST /api/users/me/password` 接口与前端入口，允许登录用户自助修改密码。
- 调整后台布局以适配移动端视图，导航与列表在窄屏下自动折叠显示。

### 2025-10-07
- 为 backend 服务新增 Docker Compose 健康检查，命中 `/routes` 确认数据接口可用。
- 在仓库根目录加入 `Makefile`，封装 `up/down/logs/test/shell` 常用命令。
- README 新增「一键命令」章节，介绍上述快捷指令。

### 2025-10-06
- 新增 `infra/nginx/conf.d/sublink.upstream.conf`，将容器内 Nginx 的入口统一代理到 `backend`。
- 提供 `docker-compose.override.yml`，默认保留 `8080:80` 并可选开启 `80:80` 暴露端口。
- README 补充部署章节，说明容器化 Nginx → backend 的统一流量入口。

### 2025-10-05
- 为 FastAPI 应用实现短链接与子域 CRUD 接口，新增 HTTP Basic 认证保护 `/api/*` 与 `/admin`。
- 增加 `/healthz`、`/{code}` 与 Host 通配跳转路由，未命中返回 404 文本。
- README 增补接口表与 `curl` 示例，便于人工自测。

### 2025-10-04
- 引入 SQLite 数据库初始化逻辑，新增 `SubdomainRedirect` 与 `ShortLink` 模型。
- 新增 `.env.example` 与文档说明，约定数据文件挂载到 `./data`。

### 2025-10-03
- 初始化变更日志，标记 server-side redirects 工作流基线。
